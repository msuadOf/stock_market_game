//! 行业报表分类层（K3，任务 9–11）：把行业账套的总账科目余额映射到准则
//! 列报行。本层只做**分类/组合**，不做完整报表生成（利润表/现金流量表/
//! 结账在任务 13）。银行（`bank`）在任务 9 落地；保险/地产（任务 10/11）
//! 以同构方式扩模块。
//!
//! 依赖方向铁律：accounting 是底座，**不得** import company——行业科目代码
//! 的单一真源放在这里（`bank::codes`），company 侧科目表构造器引用它。

pub mod bank;
