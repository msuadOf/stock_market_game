// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AccountId } from "./AccountId";
import type { OrderId } from "./OrderId";
import type { PlanId } from "./PlanId";
import type { PlanOpinion } from "./PlanOpinion";
import type { PlanStatus } from "./PlanStatus";
import type { PlanTarget } from "./PlanTarget";
import type { ResumeReason } from "./ResumeReason";
import type { ReviewConditions } from "./ReviewConditions";
import type { RevisionRecord } from "./RevisionRecord";
import type { Side } from "./Side";
import type { StockCode } from "./StockCode";
import type { Urgency } from "./Urgency";

/**
 * 可跨日的个人交易计划（K6）：唯一 per 账户+股票，版本化修订，真实成交计进度。
 */
export type TradingPlan = {
  plan_id: PlanId;
  account: AccountId;
  code: StockCode;
  direction: Side;
  target: PlanTarget;
  /**
   * 累计真实成交（按当前方向计；反向修订开新进度，旧腿真实成交留在账户里）。
   */
  filled_qty: number;
  opinion: PlanOpinion;
  confidence_bp: number;
  urgency: Urgency;
  status: PlanStatus;
  /**
   * 修订版本号，开户为 1，每次修订 +1。
   */
  version: number;
  last_revision: RevisionRecord | null;
  /**
   * 最近一次恢复携带的显式原因（恢复交易日见 last_event_trading_day）。
   */
  last_resume: ResumeReason | null;
  created_trading_day: number;
  horizon_trading_days: number;
  review: ReviewConditions;
  /**
   * 对现有母单子单的可选引用；日终清除（仅结束子单生命周期，不结束计划）。
   */
  active_child_order_id: OrderId | null;
  /**
   * 最近一次已接受事件的交易日，事件时间必须单调不减。
   */
  last_event_trading_day: number;
};
