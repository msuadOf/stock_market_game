{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认窗口能力：允许前端调用自定义 Tauri 命令 + 监听 engine-event 事件。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
