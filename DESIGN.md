---
version: alpha
name: "股票模拟游戏"
description: "面向中国 A 股玩家的高密度、移动优先模拟交易终端"
colors:
  primary: "#F52D38"
  rise: "#EF3F49"
  fall: "#009B22"
  average: "#D8A100"
  information: "#2588C7"
  background: "#F5F5F5"
  surface: "#FFFFFF"
  text: "#222222"
  muted: "#777777"
  border: "#E7E7E7"
  focus: "#126FD1"
typography:
  sans:
    fontFamily: '"Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
  numeric:
    fontFamily: '"Microsoft YaHei", "PingFang SC", system-ui, sans-serif'
rounded:
  DEFAULT: "0.1875rem"
  sm: "0.125rem"
  md: "0.25rem"
  lg: "0.5rem"
spacing:
  mobile-gutter: "0.75rem"
  dense-row: "1.6875rem"
  mobile-nav: "3.25rem"
components:
  quoteHeader: { }
  chartTabs: { }
  intradayComposite: { }
  bottomNavigation: { }
---

# 股票模拟游戏 Design System

## Overview

### Creative North Star

以同花顺手机版个股页为信息结构与密度参照：像一块实时行情仪表，而不是由大卡片组成的通用仪表盘。用户在手机上需要快速比较现价、涨跌、盘口、分时与量能，因此数字优先、边界清楚、装饰克制。

### Product context and register

- **Audience and primary job:** 中国 A 股模拟交易玩家；观察行情、判断量价、下单并复盘。
- **Target market and evidence:** 中国 A 股；产品需求、红涨绿跌规则及用户提供的同花顺截图。
- **Locale and language:** 简体中文；证券名词使用国内行情软件惯例。
- **Usage scene:** 手机竖屏高频查看，桌面端保留多面板工作台。
- **Register:** Product。熟悉度、密度与即时可读性优先。
- **Memorable signature:** 红色行情头部与紧凑的分时/盘口四象限组合。
- **Restraint:** 图表、盘口、逐笔和资金数据不使用阴影、玻璃或大圆角。
- **Anti-references:** 营销落地页、渐变玻璃卡片、低信息密度后台模板。
- **Token ownership/runtime mapping:** 本文档记录规范值；运行时由 `apps/web/src/index.css` 和 `apps/web/src/mobile/MobileStockDetail.css` 的 CSS 变量实现。

## Colors

红色表示上涨，绿色表示下跌，不沿用西方交易软件的相反语义。`primary` 仅承担移动端标题栏和选中指示；均价线使用金色，普通分时线使用近黑，信息链接使用蓝色。白色数据面与浅灰分隔线建立层级，不能用色块替代结构。

## Typography

中文使用系统黑体栈；数字启用等宽数字。移动端字号以组件容器宽度 `cqw` 与 `clamp()` 控制，主报价约为正文的 3.2 倍，盘口与逐笔约为正文的 0.85 倍。不得用整页等比例缩放模拟响应式。

## Layout

手机详情页按报价、图表周期、图表组合、独立信息菜单纵向滚动；报价摘要与图表周期之间不再重复设置“盘中交易”行情栏。分时主图与五档盘口同一行，分时量与逐笔成交同一行，左右列宽在两行保持一致。主图、时间轴和标注属于同一组件。桌面端继续使用现有多面板网格。

## Elevation & Depth

行情数据区保持平面化，以 1px 分隔线和浅灰背景分区。只有交易底页、系统遮罩等临时层允许阴影。

## Shapes

数据块以直角和小圆角为主；选中下划线可使用圆头。涨跌幅标签和明确操作按钮可使用 3–8px 圆角，不使用胶囊形容器装饰普通文本。

## Components

### Foundational visual states

所有按钮具备可见的键盘焦点；选中项同时使用文字权重和红色下划线。禁用状态降低对比并取消点击。加载、错误和空状态占据稳定区域，不移动主要控件。

### Navigation and data display

个股详情红色标题栏以 390px 宽度下约 52px 为基准，在 320–430px 容器内使用 `cqw + clamp()` 在 48–54px 之间响应；返回与切股操作保留至少 44px 点击热区，但可见切股三角、返回图形、股票名、代码、标题间距和倍速控件随容器宽度等比收放并设置上下限。两行游戏时钟（交易日、时分秒）常驻所有移动端主页面顶栏，并在详情页进入返回键与“上一只”之间；两处共享同一组件，只读取引擎权威 `Snapshot.day/tick`，并跳过 A 股午间休市。证券身份始终沿中轴居中，切股箭头在标题两侧对称分布，不与游戏时钟、机器人、倍速操作争夺视觉层级。

上方周期标签只切换分时/日 K 等图表组合；下方“看点/资讯/盘口/资金/社区/简况”是独立状态，切换周期时不得复位。分时线仅占已经过去的交易分钟，横向位置取 Rust `PriceTick.tick` 对应的权威交易分钟，右侧未来区域必须留白；连续竞价分时量固定有 240 个分钟槽位，每分钟最多一根 0.5px 细线。集合竞价量按累计值显示，在 09:15–09:30 内每分钟细分为十根同宽细线，每根取所属 6 秒槽中最后一个权威 `AuctionTick.matched_volume`，随时间向右增长，不插值或伪造额外成交量；相邻竞价细线不留空隙。竞价事件没有可成交指示价时不绘制或伪造价格线，但仍保留该时间槽的真实累计量，因此跨交易日的零成交竞价也不能整段消失。集合竞价累计量与连续竞价分钟增量拥有彼此独立的纵轴，各自以当前已经出现的最高量柱作为 100% 高度并随当前数据自适应，不能跨阶段共用绝对最大值，也不按历史或未来量级预测。午间休市区保持为空。高倍率压缩必须保留每个 6 秒竞价槽和每个连续竞价分钟槽的最后一个权威事件，不得以事件数组下标代替交易时间，也不得在跨日批次清空新交易日已经到达的采样。

详情分时图在 09:30 连续竞价前保留 09:15–09:30 的集合竞价分段，数据只来自 Rust `AuctionTick/AuctionCompleted`。价格纵轴以昨收为 0% 中轴，同时包含集合竞价和连续竞价全部有效点；上下界取最大绝对价格偏离并使用相同距离，因此严格对称。左侧显示上下界价格，右侧在同一水平位置显示正负对称涨跌幅，中轴右侧显示 `0.00%`。

列表页和个股详情始终使用同一套浅色应用主导航，保证行情、自选、交易、持仓、我的随时可达；详情内的盘口与资金由页面信息标签承载，交易入口打开统一底页。详情内容、固定主导航、遮罩和交易底页分别使用全局层级 token，禁止组件内任意增加 z-index。

移动端倍速以共享的紧凑选择按钮常驻列表与个股详情的红色顶栏，并与共享的运行切换键同组；运行时切换键显示暂停图形，暂停后显示播放图形，同时提供对应中文可访问名称，不使用机器人等与动作无关的图标。倍速选项包含 1x、1.5x、2x、3x、6x、30x、60x、180x、360x、720x、最快，按钮显示当前倍速。倍速不得重新引入独立状态栏或占用内容高度。

行情列表迷你走势图使用居中的固定宽度预览窗口，并与详情分时图共享 240 个固定交易分钟槽位；每个点只占自己的分钟位置，历史在左、最新在右，曲线随时间从左向右增长，不得按已有点数重新拉伸。早盘和午后的预览容器宽度保持一致，尚未发生的右侧分钟保留为空。纵轴以真实昨收价作为 0% 中轴，使用低对比虚线贯穿预览；范围同时包含昨收与当前窗口真实最高/最低价，并在上下各留 12% 空间，因此单边行情的零轴可自适应靠近顶部或底部。中轴与曲线之间使用当前涨跌色、由曲线侧向零轴淡出的面积渐变，填色只延伸到最新点，不得覆盖未来空白区域，也不得用与昨收无关的装饰线或填充伪装行情。

### Scroll ownership

竖屏同一时刻只有一个主滚动所有者：列表态由 `.app-grid` 滚动，详情态由 `.mobile-detail-page` 滚动；详情出现时列表滚动被冻结但保持挂载。交易底页打开后自身内部滚动，遮罩阻止背景指针与触摸滚动。固定底栏只占据安全区，不参与文档流。

### Iconography and motion

图标使用简洁线性笔画并保留文字标签。标签切换即时完成；底页进出可使用 200–300ms 状态动效，并服从 `prefers-reduced-motion`。

### Content and data visualization

金额、价格和百分比使用等宽数字与固定小数位。图表必须提供文本报价与盘口作为可访问的数据替代。错误信息说明发生位置和恢复办法，不静默降级。

所有行情量能展示——报价成交量、五档盘口、逐笔成交、分时量、K 线成交量和资金成交统计——统一使用 A 股“手”为显示单位（1 手 = 100 股），不得把引擎内部股数直接暴露到行情终端。交易委托与持仓数量仍沿用“股”，避免改变交易契约。盘口由每个权威 `PriceTick` 携带当时的前五档买卖深度并原子刷新；只更新最新价、却沿用连接时快照的盘口属于数据不同步错误。五档盘口的数量单元格按买、卖两侧各自最大挂单量绘制右对齐深度色块：卖盘淡绿、买盘淡红，极小非零档位保留最小可见宽度，数字始终位于色块上层。数量列最小宽度为 `3.2em`；色块高度取盘口行高的 72%，上下各保留 14% 的相对留白，比例随行高和字号自适应。

所有以“手”为单位的行情量以及账户金额/成交额共用同一个中文数量级格式化器：绝对值小于 1 万时最多保留两位有效小数；达到 1 万后按四位一组递归使用“万、亿、万亿、亿亿……”单位，并最多保留两位小数。小数末尾的零与空小数点必须去除，例如 `30.00万` 显示为 `30万`、`3.20万亿` 显示为 `3.2万亿`。价格、成本价和委托价属于单价，不做数量级缩写。格式化只发生在展示层，不改变引擎中的股数、分值或交易计算。

移动端分时横轴将 11:30 与 13:00 合并为同一个午休转折点，不给午休分配数据宽度。09:30–15:00 的 240 个连续竞价分钟压缩午休后等距铺开，并以 10:30、11:30/13:00、14:00 分成四个等宽时段；午休中线与其它网格线保持相同细度。集合竞价仍独立占左侧 09:15–09:30 区域，分时价格线与分时量柱共用同一横轴映射。

移动端 K 线、均线、成交量与 KDJ 共用 72 个固定横轴槽位。历史不足当前窗口容量时从左向右连续填充，不把少量数据拉伸铺满；默认密度下 K 线实体与成交量柱宽度取槽位宽度的 70%，以清楚区分相邻实心绿柱。放大档依次显示 72、48、30 根，最大放大时仍由 30 个槽位铺满 390px 横轴；柱宽最大 10px，剩余 3px 形成柱间净距，避免实体和间距失控。图、量由同一槽位几何统一控制，同一交易日的所有图形必须保持垂直对齐；只有真实数据少于当前窗口容量时，右侧才保留为空。

K 线蜡烛与成交量柱统一使用红涨绿跌形态：上涨红柱只保留红色描边并保持空心，下跌绿柱使用实心绿色；上下影线与实体同色，但必须在实体上下边界处截断，禁止贯穿空心红色实体。日内 OHLC 必须吸收每一笔真实成交价的高低极值，不能只用 tick 末价导致真实影线丢失，也不得人为伪造影线。实体上下边界只由当日开盘价与收盘价决定；昨收不参与实体绘制，跳空高开或低开时必须保留价格缺口。新交易日零成交的昨收占位不得锁定 `open`；集合竞价后的第一笔真实成交定义当日开盘价并重置占位 OHLC。

K 线窗口工具使用 27×27px 正圆按钮。放大、缩小、窗口左移、窗口右移直接操作可视历史窗口；双左箭头跳至最早历史，复位按钮返回最新数据和默认 72 槽位。到达缩放或历史边界时按钮必须进入明确禁用态。

新游戏启动时，Rust engine 为每只没有日 K 记录的股票使用该局游戏种子生成 360 个交易日的确定性 OHLCV 历史，用于立即展示完整 K 线窗口。每只股票生成历史的最后收盘价必须衔接自身初始价格；已有历史记录必须保留，不能被重新生成的数据覆盖。已完成日 K 和盘中日 K 都属于 engine 权威状态，经首次连接、重连、日界及读档后的 `Snapshot` 同步；Web 只做分→元单位转换和渲染，不生成或补齐历史。

## Do's and Don'ts

- **Do:** 保持图、量、盘口、逐笔之间的时间和列宽对应。
- **Do:** 在 320–430px 宽度内用相对字号维持与参考图一致的层级。
- **Don't:** 让五档盘口覆盖 13:00–15:00 的图表区域。
- **Don't:** 添加“模拟盘实时交易”“资金博弈/看涨人数”等已明确排除的栏位。
