# UX Contract

## Product context

- Audience: 中国 A 股模拟交易玩家
- Primary jobs: 浏览自选、查看个股量价与盘口、提交模拟委托、管理存档
- Target market(s): 中国 A 股
- Active locales: zh-CN
- Timezone/calendar policy: 游戏内交易日；A 股 09:30–11:30、13:00–15:00，共 240 个交易分钟
- Accessibility target: WCAG 2.2 AA

## Business-context sources

| Domain / scope | Authoritative source | Source type | Reviewed date |
|---|---|---|---|
| 状态所有权 | `docs/decisions/0004-frontend-state-redux-toolkit.md` | ADR | 2026-09-06 |
| 多端前端架构 | `docs/decisions/0007-three-deployment-frontend-framework.md` | ADR | 2026-09-06 |
| 核心逻辑边界 | `docs/architecture.md` | Architecture | 2026-09-06 |
| 市场展示惯例 | `DESIGN.md` 与用户提供的参考截图 | Product brief | 2026-09-06 |

## Visual contract

- Project `DESIGN.md`: `DESIGN.md`
- Token ownership model: `DESIGN.md` 记录规范，运行时 CSS 变量实现
- Runtime source: `apps/web/src/index.css`, `apps/web/src/mobile/MobileStockDetail.css`
- Supported themes: 桌面亮/暗；移动行情详情保持高对比浅色终端主题

## Canonical UI Map

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
|---|---|---|---|---|
| Scrollbar | 全局应用样式 | `DESIGN.md` | 稳定沟槽例外 | 窄屏浏览器检查 |
| Form | Blueprint 字段与现有委托面板 | 现有 `App.tsx` | 委托/条件单 | 构建与手工流程 |
| Select/Listbox | 浏览器原生 `select` | `UX-CONTRACT.md` | 倍速与远程 Publisher 模式选择均接受操作系统弹层外观与几何 | 窄屏浏览器、键盘与选中态检查 |
| Toast | 现有 `notice` live region | `App.tsx` | success/error 文案 | 屏幕阅读器语义检查 |
| Layer / overlay | `apps/web/src/index.css` 全局层级 token | `DESIGN.md` | 详情、底栏、遮罩、交易底页 | 浏览器叠层与焦点检查 |

## Navigation and responsive behavior

- 主导航：移动端底栏切换行情、自选、交易、持仓、我的；切换后退出个股详情。
- 模拟控制：移动端列表和个股详情的红色顶栏共用运行切换键与倍速选择控件；运行时显示暂停图形并执行暂停，暂停后显示播放图形并执行继续。倍速提供 1x、1.5x、2x、3x、6x、30x、60x、180x、360x、720x、最快；“最快”映射引擎无限速模式。WASM、远程服务端与 Tauri 桌面宿主通过同一 `EngineHost` 协议返回按权威 tick/现实秒采样的实际倍率，UI 不得按部署方式分叉；采样不可用时显式显示错误，且不得为倍速恢复额外的顶部状态栏。
- 远程刷新模式：远程宿主提供每客户端独立的“服务端推送 60Hz / 客户端拉取 60Hz”选择；桌面放在速度旁，移动端放在“我的/游戏状态”，本地宿主不显示无效控件。切换会重建该客户端 WS，不暂停或重建权威游戏会话；失败必须显式反馈。原生 `select` 的平台弹层外观与几何被接受。
- 游戏时间：当前交易日和 `HH:MM:SS` 常驻全部移动端主页面顶栏；进入个股详情后仍显示在返回键与“上一只”之间。两处共享同一组件，数据来自 `Snapshot.day/tick`，每 tick 为一秒，跨过 11:30–13:00 午间休市并随读档恢复。
- 列表到详情：点股票进入详情；详情头返回按钮回到原列表。
- 图表周期：分时、日 K、周 K、月 K、五日、更多只改变上方图表组合。
- 信息标签：看点、资讯、盘口、资金、社区、简况独立于图表周期，默认资金。
- 图表布局：分时/盘口和分时量/逐笔采用相同两列，不允许覆盖；详情页是文档滚动，底部导航固定并预留安全区。集合竞价累计量与连续竞价分钟量使用彼此独立的纵轴，各自把当前已经出现的最高量柱作为 100% 高度并随当前数据自适应；两阶段不得共用绝对最大值，也不得根据历史或未来量级预测。连续竞价横轴不为午休留空，11:30 与 13:00 合并为同一条普通网格线；09:30–15:00 压缩午休后由 10:30、11:30/13:00、14:00 四等分，价格线和量柱共轴。
- 行情数据真实性：五档盘口只展示引擎订单簿中的真实档位；涨停无卖单、跌停无买单或流动性不足时保留空档，不伪造挂单。报价成交量、五档数量、逐笔成交、分时量、K 线量和资金成交统计均以一手 100 股换算；新游戏 NPC 委托必须为整手，任何非整手数量按小数手精确显示，不能舍入成 0。委托输入与持仓仍以股计，防止仅为展示单位而改变交易契约。个股顶部和资金页的“当日累计”直接读取引擎日 K 的股数及逐笔对账成交额 / 成交笔数；最近 100 笔缓存只用于逐笔明细，禁止反向累加为全天统计。成交额以整数分累计并以十进制字符串跨 JSON；预置合成历史没有逐笔来源时不得伪造统计。连续竞价期间每个权威 `PriceTick` 同时刷新最新价、买卖前五档和买一/卖一，禁止盘口停留在首次连接快照。盘口量色块按卖盘、买盘各自五档中的最大挂单量归一，卖盘淡绿、买盘淡红，并保持右对齐和数字可读。
- 大数显示：所有以“手”为单位的行情量和账户金额/成交额使用共享格式化器；绝对值达到 1 万后以四位一组递归显示“万、亿、万亿、亿亿……”单位，数值最多保留两位小数，尾随零和空小数点不显示。股票价格、持仓成本价和委托价保持原始价格精度，不缩写；展示格式不得反向改变引擎数据。
- 集合竞价：默认交易日先走 09:15–09:25 集合竞价与 09:25–09:30 `PreOpen`，合计 900 tick，再走 240 分钟连续竞价。竞价指示价、虚拟成交量和开盘结果只取 Rust 引擎事件；图表把 09:15–09:25 的 100 个六秒槽完整铺满竞价显示区，09:25–09:30 `PreOpen` 不占用横轴宽度且不显示为行情数据。无交叉委托时显示空状态，不以前端估值代替。分时纵轴同时覆盖竞价与盘中全部有效点，以昨收为中心取最大绝对偏离，上下价格边界与右侧正负涨跌幅严格对称。
- 列表缩略行情：预览窗口在行情行中居中且宽度固定，并使用全天 240 个固定分钟槽位；已有行情按分钟位置从左向右增长，不按点数缩放，未来槽位保持空白。早盘和午后的容器宽度一致，阴影只覆盖已有行情至昨收中轴之间的区域。
- K 线横轴：移动端默认使用 72 个固定槽位，可缩小为 96、120 根或放大为 48、30 根；每一档均以其完整容量铺满横轴，只有真实数据不足容量时才靠左并保留右侧空白。K 线实体和成交量柱共享槽位中心；72 根及更小显示比例下柱宽取槽位的 70%，最大放大时柱宽最大 10px、柱间净距为 3px。K 线、均线、成交量与 KDJ 共用槽位，不按当前实际数据量平均拉伸。
- K 线实体：上边界取当日开盘价与收盘价的较高值，下边界取较低值；最高价与最低价只绘制影线。昨收不得补入当日实体，跳空高开或低开必须保留真实缺口。开盘前零成交 tick 只能建立昨收占位；集合竞价后的第一笔真实成交必须重置占位 OHLC，并成为当日 `open`。
- 涨跌柱形：移动端 K 线实体、日 K 成交量和分时成交量统一为上涨红色空心柱、下跌绿色实心柱；影线与对应 K 线实体同色，并在实体边界处断开，不能穿过实体内部。
- 预置历史：仅新游戏启动时，由 Rust engine 按该局种子为每只股票生成 360 个交易日 OHLCV，最后收盘价与该股初始价衔接。读档必须携带完整权威历史，缺失或截断时明确失败，不重新生成。历史和盘中蜡烛通过连接/重连/日界快照同步，Web 不生成行情。移动端默认显示最新 72 根，完整历史通过现有 K 线窗口工具访问。
- K 线窗口工具：`+ / −` 分级放大或缩小，`‹ / ›` 查看更早或更新历史，`«` 跳到最早历史，复位按钮返回最新数据与默认缩放；边界操作禁用，并保留可见焦点和中文可访问名称。
- 滚动所有权：列表态仅 `.app-grid` 主滚动；详情态冻结列表并仅让 `.mobile-detail-page` 主滚动；交易底页打开时只允许底页内部滚动。
- 层级顺序：详情内容 < 固定应用主导航 < 遮罩 < 交易底页 < 通知；遮罩拦截背景点击，Escape 关闭交易底页并把焦点还给触发按钮。
- 主导航常驻：进入个股详情后仍保留行情、自选、交易、持仓、我的；交易打开统一底页，切换其它主页面退出详情。
- 文档标题：应用级标题保持“股票模拟游戏”；后续引入路由时使用“页面 — 股票模拟游戏”。

## Flow ledger

| Operation | Trigger | Pending | Success destination | Success feedback | Failure recovery | Focus outcome | Source ref |
|---|---|---|---|---|---|---|---|
| 打开个股 | 点击行情行 | 无 | 个股详情 | 选中股票数据 | 保持列表并显示现有错误边界 | 详情返回按钮 | 当前任务 |
| 返回列表 | 详情返回按钮 | 无 | 原行情列表 | 无 | 无 | 原列表区域 | 当前任务 |
| 切换周期 | 周期标签 | 无 | 原详情滚动位置 | 标签选中态 | 保留上一张可用图 | 选中标签 | 当前任务 |
| 切换信息 | 信息标签 | 无 | 原详情滚动位置 | 标签选中态 | 显示明确占位 | 选中标签 | 当前任务 |
| 打开交易 | 底栏交易/浮动交易 | 无 | 交易底页 | 可提交委托 | Escape/遮罩关闭 | 底页首个字段 | 现有实现 |
| 切换远程刷新 | Publisher 原生选择框 | 保留现有行情 | 原页面 | 显示当前模式 | 连接失败则暂停远程会话并显示原因 | 选择框 | ADR-0005 §6 |

## Async and resilience

- 行情由统一 `EngineHost` 更新批次驱动；WASM Worker、Tauri IPC 与远程 WebSocket 都必须按
  `Event[] + 可选权威 runtime snapshot` 的相同顺序交付，UI 不得识别或分支处理部署方式。
- 远程每个客户端拥有独立 Publisher 缓冲。push 模式由服务端每 16ms 至多发布一帧，pull 模式仅响应客户端 `GetFrame`；两者使用相同 `PublisherFrame {from_seq,to_seq,events,runtime_snapshot?}`。覆盖区间允许采样后事件 seq 有空洞，但不得跳过覆盖区间；委托、错误、集合竞价结束和日界完整保留，可见成交带在三宿主统一只保留最新 100 条（与 Redux 容量一致，不是完整逐笔历史）。客户端长期停止取帧并达到 65,536 个原始事件预算时，服务端必须显式发送 `ResyncRequired` 并清空该客户端缓冲，不能继续无界占用内存。
- 首次连接、读档和明确 `ResyncRequired` 必须替换完整快照。纯行情 Publisher 帧只分派增量事件；Redux/Immer 只替换事件命中的股票分支，未命中的股票与账户分支保持引用稳定。成交、委托、集合竞价结束与日界目前仍携带权威 runtime snapshot，因此会低频整体换基线；组件已按最小状态订阅，但尚未把这些低频基线改造成账户/委托增量。
- 每个引擎 step 的 `PriceTick.tick` 是权威游戏秒；前端按每 60 tick 归入一个交易分钟。高倍率可压缩逐秒事件，但必须保留当前交易日每分钟的最后采样，且同一批次跨日后继续消费最后日界之后的数据。
- 日界清空前端分时缓存；Rust engine 提交当日 K 线并随新快照同步。错误通过现有错误页或 live region 明确显示。

### 局部刷新边界

- Publisher 与 React 绘制解耦：服务端/客户端都以 16ms（约 62.5Hz）为发布上限，模拟线程不等待浏览器绘制；一帧内同一采样槽只保留最后状态。
- 当前实现已经完成协议增量化、Redux 分支引用隔离和组件订阅边界。`App` 顶层只订阅初始化布尔值及低频玩家账户引用；时钟、行情、图表、持仓、成交和移动详情分别局部刷新，高频图表状态也由独立 Provider 承载，不再触发根组件 reconciliation。
- 组件级拆分的固定边界是：游戏时钟只订阅 `day/tick/phase`；行情容器订阅 `markets` 集合以发现动态代码，但以行引用与 AG Grid 事务隔离单股更新；分时图只订阅当前代码的历史；账户/持仓只比较玩家账户和所持代码价格，未持仓股票 tick 不触发重渲染；委托与成交带各自 slice。禁止为了图表或单行报价重新订阅完整 snapshot。
- AG Grid 行情区采用稳定代码行 ID 和按代码比较后的异步行事务更新；一个代码的 PriceTick 只提交该行的 `update`，未变化行保持对象身份。后续普通状态变更帧应改为账户/委托增量；届时全量 `setSnapshot` 才收紧为只允许初始化、读档和显式重同步。

## Verification

- Required static commands: `pnpm --filter web test`, `pnpm --filter web lint`, `pnpm --filter web build`
- Browser matrix: 320px、390px、430px 竖屏；桌面现有布局回归
- Accessibility: 原生按钮/标签语义、可见焦点、缩减动效、固定导航不遮挡焦点
- Visual regression: 与 `design/ui/mobile/` 参考及用户截图并排检查
